[package]
name = "jarnik"
version = "0.1.0"
authors = ["Craig Chen <craig.chen@duke.edu>"]
edition = "2018"

[dependencies]
csv = "*"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
criterion = "0.3"

[[bench]]
name = "bench_main"
harness = false
