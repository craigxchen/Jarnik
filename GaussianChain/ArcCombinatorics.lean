import Mathlib

/-!
Combinatorics of sliding windows over ordered arc parameters.

The live counting bound is the saturated one: when every `k + 1`-point window is forced to have
parameter span larger than the interval length, at most `k` points fit in the interval. It is
proved as `ArcCombinatorics.card_le_of_window_span_saturated` in `SaturatedWindows.lean`.
-/

namespace GaussianChain
namespace ArcCombinatorics

end ArcCombinatorics
end GaussianChain
